entity Offer {
    title String required,
    description String,
    fromDate ZonedDateTime required,
    toDate ZonedDateTime required,
    rewardPoints Integer required,
    itemQty Integer required,
    itemSku String required,
    grandTotal BigDecimal required
    image ImageBlob
}

dto * with mapstruct paginate Offer with pagination
